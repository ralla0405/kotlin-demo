package kr.io.wolverine.kotlindemo.example

fun main() {
    // filter, filterIndexed
    // map, mapIndexed, mapNotNull

    // all, none, any
    // count, sortedBy, sortedByDescending, distinctBy
    // first, firstOrNull, last, lastOrNull

    // List를 Map으로
    // groupBy, associateBy

    // 중첩 컬레션 처리 -> flatMap

    // List<List<Fruit>>를 List<Fruit>로 변경 -> flatten 평탄화
}